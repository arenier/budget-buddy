{
  "version": "0.0.1",
  "name": "budget-buddy",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run start",
    "start:dev": "turbo run start:dev --filter",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.2",
    "@repo/typescript-config": "workspace:*",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
